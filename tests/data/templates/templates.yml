templates:
  params:
    user_id:
      - name: userId
        type: uri
        data_type: int
        description: User unique identifier
        example: 23
  headers:
    json_with_auth:
      - Accept: application/json
      - Authorization: Bearer APPLICATION_TOKEN